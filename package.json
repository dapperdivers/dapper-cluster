{
  "name": "dapper-cluster",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky init",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.6"
  }
}
