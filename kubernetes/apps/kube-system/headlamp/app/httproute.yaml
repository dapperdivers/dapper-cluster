---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
spec:
  parentRefs:
    - name: internal
      namespace: network
  hostnames: ["headlamp.${SECRET_DOMAIN}"]
  rules:
    - backendRefs:
        - name: headlamp
          port: 80
