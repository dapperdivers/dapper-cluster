---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nvidia-device-plugin
spec:
  interval: 30m
  chart:
    spec:
      chart: nvidia-device-plugin
      version: 0.19.2
      sourceRef:
        kind: HelmRepository
        name: nvidia-charts
        namespace: flux-system
      interval: 15m
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  # Time-slicing sharing — no MPS control daemon, compute mode stays DEFAULT, so
  # each pod's own CUDA runtime talks directly to the card. Required for Pascal
  # (sm_60): the device plugin's MPS server is CUDA 13, which dropped sm_60 and
  # segfaulted CUDA clients. Plex (NVDEC video engines) and ollama (CUDA cores)
  # share the same cards with minimal interference.
  dependsOn:
    - name: node-feature-discovery
  values:
    image:
      repository: nvcr.io/nvidia/k8s-device-plugin
      tag: v0.19.3
    nodeSelector:
      nvidia.feature.node.kubernetes.io/gpu: "true"
    runtimeClassName: nvidia
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: feature.node.kubernetes.io/pci-0302_10de.present
                  operator: In
                  values:
                    - "true"
    config:
      map:
        default: |-
          version: v1
          flags:
            migStrategy: none
          sharing:
            timeSlicing:
              resources:
                - name: nvidia.com/gpu
                  replicas: 4
