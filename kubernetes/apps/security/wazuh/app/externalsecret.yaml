---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wazuh-secret
spec:
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  refreshInterval: 1h
  target:
    name: wazuh-secret
    template:
      engineVersion: v2
      data:
        # For indexer
        INDEXER_USERNAME: "admin"
        INDEXER_PASSWORD: "{{ .WAZUH_INDEXER_PASSWORD }}"
        INDEXER_PASSWORD_HASH: "{{ .WAZUH_DASHBOARD_PASSWORD_HASH }}"
        # For dashboard
        DASHBOARD_USERNAME: "kibanaserver"
        DASHBOARD_PASSWORD: "{{ .WAZUH_DASHBOARD_PASSWORD }}"
        DASHBOARD_PASSWORD_HASH: "{{ .WAZUH_DASHBOARD_PASSWORD_HASH }}"
        # For API
        API_USERNAME: "wazuh-wui"
        API_PASSWORD: "{{ .WAZUH_API_PASSWORD }}"
        # For authd (agent enrollment)
        AUTHD_PASS: "{{ .WAZUH_AUTHD_PASSWORD }}"
  dataFrom:
    - find:
        name:
          regexp: ^WAZUH.*
---
# The wazuh chart hardcodes "wazuh-passwords" for envFrom and authd volume
# on the manager pod, ignoring the existingSecret values.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wazuh-passwords
spec:
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  refreshInterval: 1h
  target:
    name: wazuh-passwords
    template:
      engineVersion: v2
      data:
        # authd.pass key used as volume mount for agent enrollment
        authd.pass: "{{ .WAZUH_AUTHD_PASSWORD }}"
        # env vars injected via envFrom
        INDEXER_USERNAME: "admin"
        INDEXER_PASSWORD: "{{ .WAZUH_INDEXER_PASSWORD }}"
        API_USERNAME: "wazuh-wui"
        API_PASSWORD: "{{ .WAZUH_API_PASSWORD }}"
        DASHBOARD_USERNAME: "kibanaserver"
        DASHBOARD_PASSWORD: "{{ .WAZUH_DASHBOARD_PASSWORD }}"
  dataFrom:
    - find:
        name:
          regexp: ^WAZUH.*
