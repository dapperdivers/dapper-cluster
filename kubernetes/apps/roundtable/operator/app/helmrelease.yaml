---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: roundtable-operator
spec:
  interval: 30m
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: roundtable-operator
      version: "0.7.3"
      sourceRef:
        kind: HelmRepository
        name: roundtable
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/dapperdivers/roundtable
      tag: c7602b55fa5e313db503640438143decee2b4f54
      pullPolicy: Always

    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 256Mi

    # Centralized image tags — bump here, everything updates
    images:
      piKnight:
        repository: ghcr.io/dapperdivers/pi-knight
        tag: 17deefc82794c9ef47c5fedb70ae378b8006ac7f
      dashboard:
        repository: ghcr.io/dapperdivers/roundtable-ui
        tag: 6ad1c714520d5c720b90c2cd450ab9f2796f52ca

    # Built-in planner knight — meta-mission planning
    planner:
      enabled: true
      domain: operator
      model: claude-sonnet-4-20250514
      skills:
        - operator
        - shared
      natsPrefix: fleet-a
      concurrency: 1
      taskTimeout: 600
      arsenal:
        repo: https://github.com/dapperdivers/roundtable-arsenal.git
        ref: main
        period: 5m
      envFrom:
        - secretRef:
            name: roundtable-secret

    # Dashboard — Round Table observability platform
    dashboard:
      enabled: true
      ingress:
        enabled: true
        className: internal
        host: "roundtable.${SECRET_DOMAIN}"
        annotations:
          external-dns.alpha.kubernetes.io/target: "internal.${SECRET_DOMAIN}"
      vault:
        enabled: true
        claimName: roundtable-vault
