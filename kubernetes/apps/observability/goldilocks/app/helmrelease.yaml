---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app goldilocks
spec:
  interval: 30m
  chart:
    spec:
      interval: 15m
      chart: goldilocks
      version: 10.5.0
      sourceRef:
        kind: HelmRepository
        name: fairwinds
        namespace: flux-system
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  values:
    controller:
      resources:
        requests:
          memory: 256M
        limits:
          memory: 256M
    image:
      repository: us-docker.pkg.dev/fairwinds-ops/oss/goldilocks
      tag: v4.16.0@sha256:26973551745f2c44f56020cc76e29d4d9ea61b321ee7dcc80a9924e0ec4f6153
      pullPolicy: Always
    vpa:
      enabled: true
      recommender:
        # Advisory-only (all VPAs are updateMode: Off) — no need for minute-fresh
        # recommendations. An hourly loop alone cuts status + checkpoint writes to
        # ~0.1/s (171 objects/hour), so default checkpoint storage is fine; the
        # Prometheus history backend was dropped (its queries assume the old
        # cadvisor label schema and error against kube-prometheus-stack).
        extraArgs:
          recommender-interval: 1h
    dashboard:
      enabled: true
      resources:
        requests:
          memory: 105M
        limits:
          memory: 105M
