---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app archon
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3

  values:
    controllers:
      archon:
        strategy: Recreate
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          frontend:
            image:
              repository: ghcr.io/dapperdivers/archon-dev-frontend
              tag: latest
            env:
              ARCHON_UI_HOST: "0.0.0.0"
              ARCHON_UI_PORT: &frontendPort 5173
              ARCHON_SERVER_HOST: "archon"
              ARCHON_SERVER_PORT: &serverPort 8181
              ARCHON_MCP_HOST: "archon"
              ARCHON_MCP_PORT: &mcpPort 8051
              ARCHON_AGENTS_HOST: "archon"
              ARCHON_AGENTS_PORT: &agentsPort 8052
              PORT: *frontendPort
              SERVICE_DISCOVERY_MODE: "kubernetes"
              VITE_ALLOWED_HOSTS: "archon.chelonianlabs.com,localhost,0.0.0.0"
            securityContext:
              runAsNonRoot: true
              runAsUser: 1001
              runAsGroup: 1001
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop:
                  - ALL
              seccompProfile:
                type: RuntimeDefault
            probes:
              liveness: &frontendProbes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: *frontendPort
                  initialDelaySeconds: 30
                  periodSeconds: 60
                  timeoutSeconds: 10
                  failureThreshold: 3
              readiness: *frontendProbes
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                cpu: 500m
                memory: 1Gi

          server:
            image:
              repository: ghcr.io/dapperdivers/archon-dev-server
              tag: latest
            env:
              ARCHON_SERVER_HOST: "0.0.0.0"
              ARCHON_SERVER_PORT: *serverPort
              ARCHON_UI_PORT: *frontendPort
              ARCHON_MCP_HOST: "archon"
              ARCHON_MCP_PORT: *mcpPort
              ARCHON_AGENTS_HOST: "archon"
              ARCHON_AGENTS_PORT: *agentsPort
              LOG_LEVEL: "INFO"
              SERVICE_DISCOVERY_MODE: "kubernetes"
            envFrom:
              - secretRef:
                  name: archon-supabase-secret
              - secretRef:
                  name: *app
            securityContext:
              runAsNonRoot: true
              runAsUser: 1001
              runAsGroup: 1001
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop:
                  - ALL
              seccompProfile:
                type: RuntimeDefault
            probes:
              liveness: &serverProbes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: *serverPort
                  initialDelaySeconds: 30
                  periodSeconds: 60
                  timeoutSeconds: 10
                  failureThreshold: 3
              readiness: *serverProbes
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                cpu: 1000m
                memory: 2Gi

          mcp:
            image:
              repository: ghcr.io/dapperdivers/archon-dev-mcp
              tag: latest
            env:
              ARCHON_MCP_HOST: "0.0.0.0"
              ARCHON_MCP_PORT: &mcpPort 8051
              ARCHON_SERVER_HOST: "archon"
              ARCHON_SERVER_PORT: *serverPort
              ARCHON_AGENTS_HOST: "archon"
              ARCHON_AGENTS_PORT: *agentsPort
              LOG_LEVEL: "INFO"
              SERVICE_DISCOVERY_MODE: "kubernetes"
              TRANSPORT: "sse"
            securityContext:
              runAsNonRoot: true
              runAsUser: 1001
              runAsGroup: 1001
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
              seccompProfile:
                type: RuntimeDefault
            probes:
              liveness: &mcpProbes
                enabled: false
              readiness: *mcpProbes
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                cpu: 500m
                memory: 512Mi

          agents:
            image:
              repository: ghcr.io/dapperdivers/archon-dev-agents
              tag: latest
            env:
              ARCHON_AGENTS_HOST: "0.0.0.0"
              ARCHON_AGENTS_PORT: &agentsPort 8052
              ARCHON_SERVER_HOST: "archon"
              ARCHON_SERVER_PORT: *serverPort
              ARCHON_MCP_HOST: "archon"
              ARCHON_MCP_PORT: *mcpPort
              LOG_LEVEL: "INFO"
              SERVICE_DISCOVERY_MODE: "kubernetes"
            envFrom:
              - secretRef:
                  name: archon-supabase-secret
              - secretRef:
                  name: *app
            securityContext:
              runAsNonRoot: true
              runAsUser: 1001
              runAsGroup: 1001
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
              seccompProfile:
                type: RuntimeDefault
            probes:
              liveness: &agentsProbes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: *agentsPort
                  initialDelaySeconds: 120
                  periodSeconds: 60
                  timeoutSeconds: 10
                  failureThreshold: 5
              readiness: *agentsProbes
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                cpu: 1000m
                memory: 2Gi


    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile: { type: RuntimeDefault }

    persistence:
      git-repo:
        type: emptyDir
        globalMounts:
          - path: /git
      data:
        existingClaim: ${APP}
        globalMounts:
          - path: /data
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
            readOnly: false

    service:
      app:
        controller: *app
        ports:
          http:
            port: *frontendPort
          server:
            port: *serverPort
          mcp:
            port: *mcpPort
          agents:
            port: *agentsPort

    ingress:
      app:
        annotations:
          external-dns.alpha.kubernetes.io/target: "internal.${SECRET_DOMAIN}"
        className: internal
        hosts:
          - host: &host "archon.${SECRET_DOMAIN}"
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
              - path: /api
                service:
                  identifier: app
                  port: server
              - path: /socket.io
                service:
                  identifier: app
                  port: server
        tls:
          - hosts:
              - *host
            secretName: "${SECRET_DOMAIN/./-}-tls"
