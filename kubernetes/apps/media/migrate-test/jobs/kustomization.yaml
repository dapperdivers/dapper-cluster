---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - ./migrate-movies-parallel-script.yaml  # ConfigMap with migration script
  - ./migrate-movies-parallel.yaml         # 80TB - Indexed Job: 26 pods (A-Z), 4 at a time

  # For massive folders (80TB movies, 59TB tv), use indexed parallel jobs
  # Original single job: ./migrate-movies.yaml
