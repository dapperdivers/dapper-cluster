---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - ./migrate-movies.yaml          # 80TB - will take days

  # For massive folders (80TB movies, 59TB tv), create separate dedicated jobs
