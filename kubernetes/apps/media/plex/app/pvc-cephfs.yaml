---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-cache
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 100Gi
  storageClassName: cephfs-shared

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-posters-cephfs
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: cephfs-shared
  resources:
    requests:
      storage: 100Gi  # Room to grow from 75Gi
